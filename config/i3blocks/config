# align
# color
# command
# full_text
# instance
# interval
# label
# min_width
# name
# separator
# separator_block_width
# short_text
# signal
# urgent
# Global properties
#
# The top properties below are applied to every block, but can be overridden.
# Each block command defaults to the script name to avoid boilerplate.

# Global properties
#
# The top properties below are applied to every block, but can be overridden.
# Each block command defaults to the script name to avoid boilerplate.
command=~/.config/i3blocks/blocks/$BLOCK_NAME
separator_block_width=20
markup=pango

full_text=|
align=center
separator=false
color=#6a8799
separator_block_width=7

# Update check
#
[updates]
interval=900
min_width=A A
color=#928374

[separator]

# CPU usage
#
# The script may be called with -w and -c switches to specify thresholds,
# see the script for details.
[cpu]
interval=10
min_width=100.00%
color=#928374

[separator]

# Memory usage
#
# The type defaults to "mem" if the instance is not specified.
[memory]
interval=30
color=#928374

[separator]

# Disk usage
#
# The directory defaults to $HOME if the instance is not specified.
# The script may be called with a optional argument to set the alert
# (defaults to 10 for 10%).
#[disk]
#label=
#interval=30
#color=#83a598
#separator_block_width=10

# my root / partition
[disk]
instance=/home
interval=60
color=#928374

# d plate
[disk]
instance=/mnt/d
interval=60
color=#928374

# e plate
[disk]
instance=/mnt/e
interval=60
color=#928374

[separator]

[volume]
instance=alsa_output.pci-0000_0b_00.4.analog-stereo
interval=once
signal=10
max_width=🔊 100%
color=#928374

[volume]
instance=alsa_output.usb-046d_G435_Wireless_Gaming_Headset_V001008005.1-01.analog-stereo
interval=once
signal=10
max_width=🎧 100%
color=#928374

[separator]

[clock]
interval=1
color=#928374

#lightred #fb4934
#red #cc241d
#green #98971a
#lightgreen #b8bb26
#yellow #d79921
#lightyellow #fabd2f
#blue #458588
#purple #b16286
#aqua #689d68
#
